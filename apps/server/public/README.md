# Socket.io Connection Test

- `test.html` is a minimal client that connects to the backend.
- Opens a WebSocket connection to `http://localhost:3000`.
- Emits `test-event` and receives `test-response`.
- No authentication, rooms, or drawing implemented yet — this is just a connection test.
