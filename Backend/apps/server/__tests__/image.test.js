const request = require('supertest');
const app = require('../app.js');

describe('Image Sanitization Test', () => {
  test('Invalidates an image when detected an artificially corrupt file', () => {});
});
